export const analyticsConfig = {
  /**
   * Set to false to disable PostHog analytics and the cookie consent banner.
   * When disabled, no tracking scripts will be loaded and no cookie popup will appear.
   */
  enabled: true,
};
